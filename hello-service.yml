server:
  port: 8081

application:
  message: Hello Service From Config Server

resilience4j:
  circuitbreaker:
    instances:
      greetService:
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s  
