server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:

        - id: hello-service
          uri: http://localhost:8081
          predicates:
            - Path=/hello/**

        - id: greet-service
          uri: http://localhost:8082
          predicates:
            - Path=/greet/**
